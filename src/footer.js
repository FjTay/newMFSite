import React from "react";

const Footer = (props) => {

    return (
        <>
            <div 
                id="footer" 
            >FOOTER</div>
            {console.log("Footer")}
        </>
    )
};

export default React.memo(Footer);